<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:orientation="horizontal" >

  <!-- <1> -->
  <FrameLayout android:id="@+id/preview"
    android:layout_weight="1"
    android:layout_width="1150dip"
    android:layout_height="match_parent">
  </FrameLayout>
  <!-- <3> -->
  <LinearLayout android:layout_width="match_parent" android:layout_height="match_parent" android:orientation="vertical">"
  <Button android:layout_width="150dip"
    android:layout_height="wrap_content" android:id="@+id/ZoomInButton"
    android:text="Zoom In" android:layout_gravity="center" 
    android:gravity="center"
    android:layout_marginTop="50dip"></Button>"
  <Button android:layout_width="150dip"
    android:layout_height="wrap_content" android:id="@+id/ZoomOutButton"
    android:text="Zoom Out" android:layout_gravity="center"
	android:gravity="center"
	android:layout_marginTop="20dip"></Button>
  <Button android:layout_width="150dip"
    android:layout_height="wrap_content" android:id="@+id/buttonClick"
    android:text="Take Picture" android:layout_gravity="center" android:layout_marginTop="100dip" />
  </LinearLayout>
</LinearLayout>
